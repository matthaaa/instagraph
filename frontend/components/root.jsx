import React from 'react';
import {Provider} from 'react-redux';
import {HashRouter} from 'react-router-dom';
import App from './app';

// TODO: Add Provider and HashRouter.
const Root = ({store}) => (
  <App/>
)

export default Root;
